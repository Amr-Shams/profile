{
  "name": "profile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "dune exec bin/main.exe",
    "preview": "python3 -m http.server 3000 -d build",
    "deploy": "wrangler deploy"
  }
}
